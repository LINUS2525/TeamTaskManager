spring:
  datasource:
    url: jdbc:h2:mem:team_task_manager_test;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  seed:
    enabled: false
